---
description: Auto-run Supabase SQL migrations and database commands
---
// turbo-all

## Run Supabase Migrations
1. Navigate to Supabase Dashboard SQL Editor
2. Paste and run migration SQL files

## Common Database Operations
3. Check user roles: `SELECT * FROM public.user_roles;`
4. Check profiles: `SELECT * FROM public.profiles;`
5. Check auth users: `SELECT id, email FROM auth.users;`
