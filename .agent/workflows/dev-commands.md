---
description: Auto-run development server and common npm commands
---
// turbo-all

## Start Development Server
1. Run `npm run dev` to start the Vite development server

## Build for Production
2. Run `npm run build` to build the production bundle

## Preview Production Build
3. Run `npm run preview` to preview the production build locally
